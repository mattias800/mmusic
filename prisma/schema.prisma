datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Media {
  id        String   @id @default(uuid())
  title     String
  artist    String?
  album     String?
  year      Int?
  path      String
  createdAt DateTime @default(now())
} 